name: pet_boarding_app
description: Aplikasi Tempat Penitipan Hewan
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  qr_flutter: ^4.1.0  # Tambahkan untuk QR Code
  flutter:
    sdk: flutter
  http: ^1.1.0
  shared_preferences: ^2.2.2
  image_picker: ^1.0.5  # Untuk memilih gambar
  intl: ^0.18.1
  flutter_map: ^6.1.0
  latlong2: ^0.9.0
  url_launcher: ^6.3.1 
  fl_chart: ^0.68.0  # Tambahkan untuk grafik



dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true